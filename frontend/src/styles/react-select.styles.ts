export const timezoneStyles = {
  container: (styles: any) => ({
    ...styles,
    width: "100%",
    color: "#1F1F1F",
    fontSize: "1rem",
  }),
  control: () => ({
    backgroundColor: "#FDFEFF",
    color: "#1F1F1F",
    paddingLeft: "2.5rem",
    height: "48px",
    cursor: "pointer",
    border: "1px solid #6528F7",
    outline: 0,
    borderRadius: "0.3125rem",
    display: "flex",
    alignItems: "center",
    justifyContents: "space-between",
  }),
  option: () => ({
    padding: "0.5rem 1rem",
    backgroundColor: "#FDFEFF",
    fontSize: "1rem",
    fontWeight: "400",
    color: "#1F1F1F",
    cursor: "pointer",
  }),
  singleValue: (styles: any) => ({
    ...styles,
    color: "#1F1F1F",
  }),
  placeholder: (provided: any) => ({
    ...provided,
    color: "#1F1F1F66",
    fontSize: "1rem",
  }),
};

export const categoriesStyles = {
  container: (styles: any) => ({
    ...styles,
    width: "100%",
    color: "#1F1F1F",
    fontSize: "1rem",
  }),
  control: () => ({
    backgroundColor: "#FDFEFF",
    color: "#1F1F1F",
    padding: "0 6px",
    height: "48px",
    cursor: "pointer",
    border: "1px solid #6528F7",
    outline: 0,
    borderRadius: "0.3125rem",
    display: "flex",
    alignItems: "center",
    justifyContents: "space-between",
  }),
  option: () => ({
    padding: "0.5rem 1rem",
    backgroundColor: "#FDFEFF",
    fontSize: "1rem",
    fontWeight: "400",
    color: "#1F1F1F",
    cursor: "pointer",
  }),
  placeholder: (styles: any) => ({
    ...styles,
    color: "#1F1F1F66",
    fontSize: "1rem",
  }),
};

export const ReviewTimezoneStyles = {
  container: (styles: any) => ({
    ...styles,
    width: "100%",
    color: "#1F1F1F",
    fontSize: "1rem",
  }),
  control: () => ({
    backgroundColor: "#f4f2f4",
    color: "#1F1F1F",
    paddingLeft: "2.5rem",
    height: "48px",
    cursor: "pointer",
    border: "0",
    outline: 0,
    borderRadius: "0.3125rem",
    display: "flex",
    alignItems: "center",
    justifyContents: "space-between",
  }),
  option: () => ({
    padding: "0.5rem 1rem",
    backgroundColor: "#FDFEFF",
    fontSize: "1rem",
    fontWeight: "400",
    color: "#1F1F1F",
    cursor: "pointer",
  }),
  singleValue: (styles: any) => ({
    ...styles,
    color: "#1F1F1F",
  }),
  placeholder: (provided: any) => ({
    ...provided,
    color: "#1F1F1F66",
    fontSize: "1rem",
  }),
};

export const ReviewCategoriesStyles = {
  container: (styles: any) => ({
    ...styles,
    width: "100%",
    color: "#1F1F1F",
    fontSize: "1rem",
  }),
  control: () => ({
    backgroundColor: "#f4f2f4",
    color: "#1F1F1F",
    paddingInline: "6px",
    height: "48px",
    cursor: "pointer",
    border: "0",
    outline: 0,
    borderRadius: "0.3125rem",
    display: "flex",
    alignItems: "center",
    justifyContents: "space-between",
  }),
  option: () => ({
    padding: "0.5rem 1rem",
    backgroundColor: "#FDFEFF",
    fontSize: "1rem",
    fontWeight: "400",
    color: "#1F1F1F",
    cursor: "pointer",
  }),
  singleValue: (styles: any) => ({
    ...styles,
    color: "#1F1F1F",
  }),
  placeholder: (styles: any) => ({
    ...styles,
    color: "#1F1F1F66",
    fontSize: "1rem",
  }),
};

export const BuyTicketSelectStyle = {
  container: (provided: any) => ({
    ...provided,
    width: "100%",
    color: "#1F1F1F",
    fontSize: "1rem",
  }),
  control: () => ({
    backgroundColor: "#FDFEFF",
    color: "#1F1F1F",
    padding: "0 6px",
    height: "48px",
    cursor: "pointer",
    border: "2px solid #d1d1d1",
    outline: 0,
    borderRadius: "0.3125rem",
    display: "flex",
    alignItems: "center",
    justifyContents: "space-between",
  }),
  option: () => ({
    padding: "0.5rem 1rem",
    backgroundColor: "#FDFEFF",
    fontSize: "1rem",
    fontWeight: "400",
    color: "#1F1F1F",
    cursor: "pointer",
  }),
  singleValue: (styles: any) => ({
    ...styles,
    color: "#1f1f1f",
  }),
  placeholder: (provided: any) => ({
    ...provided,
    color: "#1F1F1F66",
    fontSize: "1rem",
  }),
};
